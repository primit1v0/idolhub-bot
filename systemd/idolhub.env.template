# =========================================================
# IDOLHUB ENVIRONMENT CONFIGURATION
# Copy to a machine-local path such as ~/.config/idolhub/.env.
# Never commit the populated file.
# =========================================================

# 1. Telegram
TELEGRAM_BOT_TOKEN="REPLACE_ME"

# 2. Gemini Configuration (Default aktif)
GEMINI_API_KEY="REPLACE_ME"

# 3. OpenAI Configuration
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_API_KEY="REPLACE_ME"

# 4. OpenAI Codex (Opsional, jika provider=openai_codex)
OPENAI_CODEX_TOKEN="REPLACE_ME"

# 5. GitHub Copilot (Opsional, jika provider=github_copilot)
GITHUB_COPILOT_TOKEN="REPLACE_ME"
